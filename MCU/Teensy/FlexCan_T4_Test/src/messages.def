#include "FuncTest.h"
#include "pp_help.def"

// X(id, function)
#define CAN_MESSAGES \
    X(0, canSniff)   \
    X(1, canSniff)   \
    X(2, canSniff)   \
    X(3, canSniff)   \
    X(4, canSniff)   \
    X(5, canSniff)   \
    X(6, canSniff)   \
    X(7, canSniff)   \
    X(8, canSniff)   \
    X(9, canSniff)   \
    X(10, canSniff)
